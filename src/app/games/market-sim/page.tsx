import MarketSimClient from './MarketSimClient';

export const metadata = {
  title: 'Virtual Trading Terminal | Arpit Agarwala',
  description: 'Simulate trading stocks, crypto, and forex with realistic chart data and portfolio tracking.',
};

export default function MarketSimPage() {
  return <MarketSimClient />;
}
