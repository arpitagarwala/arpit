import AsmrClient from './AsmrClient';

export const metadata = {
  title: 'ASMR Relaxing Rain | Arpit Agarwala',
  description: 'A customizable ambient rain simulator. Adjust the intensity, speed, and volume for focus or relaxation.',
};

export default function AsmrPage() {
  return <AsmrClient />;
}
