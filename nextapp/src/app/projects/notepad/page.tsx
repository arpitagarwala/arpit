import NotepadClient from './NotepadClient';

export const metadata = {
  title: 'Notepad | Arpit Agarwala',
  description: 'Free browser notepad — write, format, and save notes locally.',
};

export default function NotepadPage() {
  return <NotepadClient />;
}
